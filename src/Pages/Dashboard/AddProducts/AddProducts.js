import { Typography } from "@mui/material";
import React from "react";

const AddProducts = () => {
  return (
    <div>
      <Typography>Add A product that you want</Typography>
    </div>
  );
};

export default AddProducts;
